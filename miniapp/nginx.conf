server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
    gzip_min_length 1024;

    location ~ ^/(auth|users|translate|chats|stats|billing)(/|$) {
        proxy_pass https://trans-app-api-production.up.railway.app;
        proxy_ssl_server_name on;
        proxy_ssl_name trans-app-api-production.up.railway.app;
        proxy_ssl_verify off;
        proxy_set_header Host trans-app-api-production.up.railway.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

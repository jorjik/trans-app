# Backend base URL (no trailing slash)
VITE_API_URL=http://localhost:8000

# Optional: raw initData string from Telegram.WebApp.initData (browser testing only).
# Must be a valid signed query string from the Mini App; a JWT or random string will not work.
VITE_DEV_TOKEN=
